{# Extend page template #}
{% extends 'templates/page.twig' %}

{% set title = 'Login' %}
{% set breadcrumb_title = 'Login' %}
{% set hideTitle = 1 %}
{% set hideDesc = 1 %}

{# Set labels #}
{% set labels = {
'email_address': 'Email Address',
'password': 'Password'
} %}

{% block page_content %}
    <div class="text-center">
        <h1>
            Sign in
        </h1>
        <h4 class="subheader">
            Manage your account.
        </h4>
    </div>
    <div class="row">
        <div class="column medium-push-2 medium-8 large-push-3 large-6">
            {{ theme.errors(errors) }}
            <form action="{{ global.current_url }}" method="post" class="form-with-validation clearblock account-login">
                <label class="field-block">
                    <span class="field-label">Email address</span>
                    {{ html.input('email', '', 'required','', 'email' ) }}
                </label>
                <label class="field-block">
                    <span class="field-label">Password</span>
                    {{ html.input('password', '', 'required', '', 'password') }}
                </label>
                <button type="submit" class="button expanded">Sign in</button>
                <div class="text-center">
                    <p>
                        <a href="/account/forgotten-password" class="underline">Forgotten password?</a>
                    </p>
                    <p>
                        Don't have an account with us? <a class="highlight underline" href="/account/create">Create one now</a>
                    </p>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
